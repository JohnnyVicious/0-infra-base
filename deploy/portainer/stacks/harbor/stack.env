# Harbor stack parameters
HARBOR_HOSTNAME=harbor.example.local
HARBOR_HTTP_PORT=8080
HARBOR_HTTPS_PORT=8443
HARBOR_ADMIN_PASSWORD=ChangeMe123!

# Shared secrets (generate strong random values)
CORE_SECRET=generate-a-random-32-byte-string
JOBSERVICE_SECRET=generate-another-random-32-byte-string
REGISTRY_HTTP_SECRET=generate-a-random-32-byte-string
TRIVY_SECRET=generate-a-random-32-byte-string

# Database credentials
POSTGRESQL_USERNAME=harbor
POSTGRESQL_PASSWORD=harbor_password
POSTGRESQL_DATABASE=registry

# Redis
REDIS_PASSWORD=harbor_redis_password

# Optional proxy support
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=core,portal,registry,registryctl,jobservice,trivy,chartmuseum,postgresql,redis,proxy,127.0.0.1,localhost
